# vim:set et sw=2 ts=8 ft=zsh:

local -a reply
autoload -Uz _fzf_complete chpwd_recent_filehandler
chpwd_recent_filehandler $reply

_fzf_complete "" "$@" < <(
  echo "$(for line in $reply; do echo $line; done)"
)
